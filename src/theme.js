import {extendTheme} from '@chakra-ui/react'

const theme = extendTheme({
    colors: {
        primary: '#495e57',
        secondary: {
            100: '#f4ce14',
            200: '#e5c113',
            400: '#e5c113'
        },
    }
})

export default theme
